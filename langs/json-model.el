;;; json-model.el --- Summary
;;; Commentary:
;;; Code:

(require 'json-mode)

(defun trivialfis/json ()
  "JSON configuration."
  (setq-default js-indent-leve 2))
(provide 'json-model)
;;; json-model.el ends here
